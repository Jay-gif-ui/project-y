import { Footer } from "@/components/footer"; import { Navbar } from "@/components/navbar"; import { MovieGrid } from "@/components/movie-grid"; import { searchMedia } from "@/lib/tmdb";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;const query=q.trim();const result=query?await searchMedia(query):undefined;return <><Navbar/><main className="shell search-page"><p className="eyebrow">Search</p><h1>{query?`Results for “${query}”`:"Search the stories you love"}</h1>{!query?<p className="section-status">Enter a movie or TV show title above to begin.</p>:result&&"error"in result?<p className="section-status" role="alert">Search is temporarily unavailable. Please try again.</p>:result?.data?.length?<MovieGrid movies={result.data}/>:<p className="section-status">No movies or TV shows matched “{query}”. Try another title.</p>}</main><Footer/></>}
